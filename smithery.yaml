runtime: container
build:
  dockerfile: Dockerfile
  dockerBuildPath: .
startCommand:
  type: http
  configSchema:
    type: object
    properties:
      debug:
        type: boolean
        default: false
        description: "Enable debug logging"